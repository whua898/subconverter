# 测试 CI → SSH 部署全流程 param( [string]$TestSubscriptionUrl = "https://raw.githubusercontent.com/xxx/xxx.txt" ) function Log-Info { param([string]$Msg) Write-Host "[TEST] $(Get-Date -Format 'HH:mm:ss') $Msg" } Log-Info "=== 开始测试部署流程 ===" Log-Info "请手动执行: .\deploy.ps1 -SshHost user@vps-ip -RemoteComposeDir /opt/subconverter" Log-Info "预期顺序: Git Push → Actions 编译 → SSH 更新 → 健康检查 → 订阅验证" Log-Info "### 下一步操作 ###" Log-Info "1. 在 GitHub 仓库添加 Secrets:" Log-Info " - SSH_PRIVATE_KEY (VPS 的私钥)" Log-Info " - VPS_HOST (如 user@1.2.3.4)" Log-Info "2. 检查 deploy.ps1 中的参数是否匹配你的环境" Log-Info "3. 如需测试，可先用 --DryRun 参数做 dry-run（如需扩展）" Log-Success "=== 测试脚本就绪 ==="