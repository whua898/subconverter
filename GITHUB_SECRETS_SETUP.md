# GitHub Secrets 配置指南 ## 1. 进入仓库 Settings -> Secrets and variables -> Actions ## 2. 添加以下 Secrets | Name | Value | 说明 | |------|-------|------| | SSH_PRIVATE_KEY | VPS 私钥内容 | 用于 SSH 登录 VPS | | VPS_HOST | user@1.2.3.4 | VPS SSH 地址 | | GITHUB_TOKEN | ${{ secrets.GITHUB_TOKEN }} | 自动提供，用于 Actions 调用 API | ## 3. 私钥导入方式 在本地 PowerShell 执行： ```powershell Get-Content ~/.ssh/subconverter_deploy | Set-Clipboard ``` 粘贴到 Secret Value 输入框。 ## 4. 验证 推送代码后，GitHub Actions 应能自动获取 Secrets 并执行部署流程。